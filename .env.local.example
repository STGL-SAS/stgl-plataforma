NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Llave secreta Bold (Botón de pagos / Link de pago) — firma webhooks.
# Preview/Development: llave de pruebas. Production: llave real.
BOLD_WEBHOOK_SECRET=

# Opcional — consulta fallback GET /payments/webhook/notifications/{payment_id}
BOLD_LLAVE_IDENTIDAD=

# Microsoft Graph / OneDrive (Fase 6) — valores en Vercel; ver guía Entra
MSGRAPH_CLIENT_ID=
MSGRAPH_CLIENT_SECRET=
MSGRAPH_TENANT=
MSGRAPH_REDIRECT_URI=http://localhost:3000/api/onedrive/auth/callback
